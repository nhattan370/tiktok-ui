const images = {
    //take file which have the same level
    logo: require('./logo.svg').default,
    avatar: require('./snow.webp'),
    no_image: require('./no_image.png'),
    background: require('./background.jpg')
}
export default images;